spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/taskmanager
    username: taskuser
    password: taskpass

  jpa:
    hibernate:
      ddl-auto: update

  kafka:
    bootstrap-servers: kafka:9092

server:
  port: 8080